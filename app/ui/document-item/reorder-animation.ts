/**
 * How long a row takes to arrive at its new position. Kept in sync with
 * `--animate-row-shift` / `--animate-row-settle` in `app/app.css`, which do
 * the actual animating.
 */
export const REORDER_DURATION_MS = 180;
